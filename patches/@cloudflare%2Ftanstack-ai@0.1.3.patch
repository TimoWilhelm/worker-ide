diff --git a/dist/chunk-53RO5I22.cjs b/dist/chunk-53RO5I22.cjs
--- a/dist/chunk-53RO5I22.cjs
+++ b/dist/chunk-53RO5I22.cjs
@@ -132,7 +132,7 @@ function createWorkersAiBindingFetch(binding, options) {
       });
     }
     const responseObj = typeof result === "object" && result !== null ? result : { response: String(result) };
-    const responseText = typeof responseObj.response === "string" ? responseObj.response : "";
+    const responseText = typeof responseObj.response === "string" ? responseObj.response : typeof responseObj.response === "object" && responseObj.response !== null ? JSON.stringify(responseObj.response) : "";
     const message = {
       role: "assistant",
       content: responseText
diff --git a/dist/chunk-727MRLUF.js b/dist/chunk-727MRLUF.js
--- a/dist/chunk-727MRLUF.js
+++ b/dist/chunk-727MRLUF.js
@@ -132,7 +132,7 @@ function createWorkersAiBindingFetch(binding, options) {
       });
     }
     const responseObj = typeof result === "object" && result !== null ? result : { response: String(result) };
-    const responseText = typeof responseObj.response === "string" ? responseObj.response : "";
+    const responseText = typeof responseObj.response === "string" ? responseObj.response : typeof responseObj.response === "object" && responseObj.response !== null ? JSON.stringify(responseObj.response) : "";
     const message = {
       role: "assistant",
       content: responseText
diff --git a/dist/chunk-DTGQYEJ2.cjs b/dist/chunk-DTGQYEJ2.cjs
--- a/dist/chunk-DTGQYEJ2.cjs
+++ b/dist/chunk-DTGQYEJ2.cjs
@@ -157,7 +157,7 @@ var WorkersAiTextAdapter = class extends _adapters.BaseTextAdapter {
     this.client = buildWorkersAiClient(config);
   }
   async *chatStream(options) {
-    const { systemPrompts, messages, tools, temperature, model } = options;
+    const { systemPrompts, messages, tools, temperature, maxTokens, model } = options;
     const openAIMessages = buildOpenAIMessages(systemPrompts, messages);
     const openAITools = buildOpenAITools(tools);
     const timestamp = Date.now();
@@ -179,6 +179,7 @@ var WorkersAiTextAdapter = class extends _adapters.BaseTextAdapter {
           messages: openAIMessages,
           tools: openAITools,
           temperature,
+          max_tokens: maxTokens,
           stream: true,
           stream_options: { include_usage: true }
         });
@@ -191,7 +192,8 @@ var WorkersAiTextAdapter = class extends _adapters.BaseTextAdapter {
           model: _nullishCoalesce(model, () => ( this.model)),
           messages: openAIMessages,
           tools: openAITools,
-          temperature
+          temperature,
+          max_tokens: maxTokens
         });
         yield {
           type: "RUN_STARTED",
diff --git a/dist/chunk-HDDZEGLC.js b/dist/chunk-HDDZEGLC.js
--- a/dist/chunk-HDDZEGLC.js
+++ b/dist/chunk-HDDZEGLC.js
@@ -157,7 +157,7 @@ var WorkersAiTextAdapter = class extends BaseTextAdapter {
     this.client = buildWorkersAiClient(config);
   }
   async *chatStream(options) {
-    const { systemPrompts, messages, tools, temperature, model } = options;
+    const { systemPrompts, messages, tools, temperature, maxTokens, model } = options;
     const openAIMessages = buildOpenAIMessages(systemPrompts, messages);
     const openAITools = buildOpenAITools(tools);
     const timestamp = Date.now();
@@ -179,6 +179,7 @@ var WorkersAiTextAdapter = class extends BaseTextAdapter {
           messages: openAIMessages,
           tools: openAITools,
           temperature,
+          max_tokens: maxTokens,
           stream: true,
           stream_options: { include_usage: true }
         });
@@ -191,7 +192,8 @@ var WorkersAiTextAdapter = class extends BaseTextAdapter {
           model: model ?? this.model,
           messages: openAIMessages,
           tools: openAITools,
-          temperature
+          temperature,
+          max_tokens: maxTokens
         });
         yield {
           type: "RUN_STARTED",
